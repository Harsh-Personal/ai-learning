Tokens and what I understand from them so basically the tokens are called the currency of LLMY because at the unit level every word or character or anything we give to LLM is converted into a token which eventually is converted into an ID and diet ID is referred to buy LLM so it is not necessary. That one word will be one token and a word which has some more space or has any other character with it or any symbol with it will be treated as a different token altogether so basically just hello and hello! Will be treated us two different tokens and will be given separate token IDs so these open IDs are what referred to buy LLM to generate the expected output. They also generate output in which are converted to text as we discussed. Common words are one token because LLM answered, but their words are multiple words in a single word they get converted to separate for better understanding of LLM.

Why token matter for AI testing because it's like as we said like it is the currency of AI and it's like the cost factor of matters so suppose we have two models so and we have the same question one model is giving the answer to that same question in life hundred words that is giving in 300 words then model a is very much better. It is saving tokens and ultimately cost and giving the right answer so model A will be in that case also context limit also matters because the currency is open so every model has their own token limits contact limit basically so for long documented we will need more context limited like more contact limit so this is also a factor lecture. We can't have a model with very less context limit or cooked open limit and then ask it to analyze that document which requires more token so that is also one of the factors we need to consider

what I learned in this week is I I didn't experiment today with the temperatures where I was learning about temperature so I asked the same question 10 times but with temperature zero but it gave different answers, but those were very close so it's like it mostly picked the highest probability to open which makes it more deterministic so this is also less cost because they had to generate less tokens provided if temperature when they have to search for sample from all possible tokens and then can generate random tokens or words basically which is not much efficient also, one of the cases I learned as we did an air Canada case so I think that fits in one of the learnings we do that maybe their policy document was very lame and the model didn't have enough context to get to get through the entire policy and that's why it gave from whatever info it had and assumed remaining so Assuming leads to hallucinations or basically wrong information in which could have real cost implication as we saw in that case so I think as an as that, I should definitely consider all these points when we are evaluating a model based on the answer, it is giving the token efficiency consistency in context, windows, and response length variance so that we can set the best model for whatever we are doing. I'm not sure if there is any alternative to opens or if there is any LLM which doesn't use token or are they always based on it or if there is any alternative to it could you guide me in that?
